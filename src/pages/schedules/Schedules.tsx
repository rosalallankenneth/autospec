import React from "react";
import { Box } from "@mui/material";
import MainLayout from "@/layouts/MainLayout";

const Schedules = () => {
	return <Box>Schedules</Box>;
};

Schedules.getLayout = (page: any) => <MainLayout>{page}</MainLayout>;

export default Schedules;
